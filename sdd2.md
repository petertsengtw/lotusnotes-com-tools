# SDD2 — 自動打卡：放假日跳過功能

規格驅動開發（Spec-Driven Development）文件。本階段僅開發一項功能：**讓 `auto_checkin.py` 在放假日自動跳過整個打卡流程**。

---

## 1. 背景與問題

`auto_checkin.py` 由 Windows 工作排程器每天固定時間（08:00 簽到 / 17:00 簽退）自動執行，流程包含：

1. Lotus Notes 簽到退（寫入 `hmsign.nsf`）
2. Portal 上網認證登入
3. LINE 完成通知

目前腳本沒有「今天是否該打卡」的判斷邏輯——只要排程時間到就一定執行。當使用者請假、放假時，這個自動化會誤動作，寫入不該存在的打卡紀錄。而 `hmsign.nsf` 的打卡文件**建立後無法刪除或修改**（ACL 限制，見 README 注意事項），誤打卡會造成無法復原的錯誤紀錄。

## 2. 目標 / 非目標

**目標**
- 提供一個使用者可自行維護的「放假日清單」檔案。
- 執行 `auto_checkin.py` 前，先檢查今天日期是否在放假日清單中。
- 若今天是放假日，**整個流程（打卡、Portal 登入、LINE 通知）全部跳過**，不做任何動作，僅寫入 log。
- 不影響現有簽到 / 簽退功能在非放假日的行為。

**非目標（本階段不做）**
- 不做週末 / 國定假日自動判斷（不串接行事曆或假日 API）。
- 不做「補打卡」或「假日仍打卡但標記例外」的邏輯。
- 不做 GUI 或互動式清單編輯工具，使用者直接用文字編輯器維護檔案。

## 3. 使用者情境

> 我平常靠工作排程器每天自動打卡。但如果我今天請假 / 放假，我希望前一天先在一個檔案裡寫下這個日期，隔天腳本執行時看到今天在清單裡，就整個流程都不執行，什麼都不用做（也不用收到 LINE 通知），日誌照常記錄一筆「跳過」即可。

## 4. 設計

### 4.1 放假日清單檔案

| 項目 | 內容 |
|---|---|
| 檔名 | `holidays.txt` |
| 位置 | 專案根目錄（與 `auto_checkin.py` 同層） |
| 編碼 | UTF-8 |
| 格式 | 純文字，**一行一個日期**，格式 `YYYY-MM-DD` |
| 註解 | 行首 `#` 或日期後接 `#` 皆視為註解，會被忽略 |
| 空白行 | 忽略 |

範例 `holidays.txt`：

```
# 2026 年放假日清單
2026-08-21          # 特休
2026-08-24
2026-09-28          # 教師節補假

2026-10-10           # 國慶日
```

**檔案不存在時的行為：** 視為「沒有任何放假日」，正常執行打卡流程，並在 log 寫入一則提醒（不中斷、不報錯），方便使用者知道目前沒有套用放假日清單。

**格式錯誤行的行為：** 該行略過並寫入 log 警告，不中斷整體執行（避免一行打錯字就讓當天該打卡卻沒打卡）。

### 4.2 判斷邏輯

新增函式：

```python
def is_holiday(check_date: date) -> bool:
    """讀取 HOLIDAY_FILE，判斷 check_date 是否為放假日"""
```

- 讀取 `HOLIDAY_FILE`（新增設定常數，路徑寫法比照現有 `LOG_FILE`）。
- 逐行解析，去除 `#` 之後的註解與空白，剩餘內容以 `datetime.strptime(x, "%Y-%m-%d").date()` 解析。
- 解析失敗的行：`logging.warning(...)` 後跳過該行，不影響其他行。
- 回傳 `check_date` 是否存在於解析出的日期集合中。
- 日期基準使用**執行當下的系統本地日期**（`datetime.now().date()`），與現有 `+timedelta(hours=8)` 的 UTC 校正邏輯無關（那段校正只作用在寫入 Notes 文件的時間欄位，不影響本機判斷用的日期）。

### 4.3 主流程串接

在 `if __name__ == "__main__":` 內，於呼叫 `checkin()` **之前**加入判斷：

```python
today = datetime.now().date()
if is_holiday(today):
    logging.info(f"{today} 為放假日，跳過本次打卡流程")
    print(f"[{datetime.now():%Y-%m-%d %H:%M:%S}] 今日為放假日，跳過打卡")
    sys.exit(0)

checkin("1" if action == "in" else "0")
portal_login()
send_line_message(f"[自動簽到退] {label} 完成 {datetime.now():%Y-%m-%d %H:%M:%S}")
```

- `sys.exit(0)`（非錯誤結束碼），確保工作排程器不會把「放假日跳過」誤判為執行失敗。
- 不呼叫 `checkin()`、`portal_login()`、`send_line_message()` 三者中的任何一個。
- 不發送 LINE 通知（依需求為靜默跳過，僅記錄於 `checkin.log`）。

### 4.4 流程圖（新增分支）

```mermaid
flowchart TD
    A([Windows 工作排程器]) --> B[auto_checkin.py 啟動]
    B --> C{今天在\nholidays.txt?}
    C -- 是，放假日 --> SKIP[記錄 log：跳過]
    SKIP --> END([結束，不執行任何步驟])
    C -- 否 --> D[1. Lotus Notes 簽到退]
    D --> E[2. Portal 登入]
    E --> F[3. LINE 通知]
    F --> END2([結束])
```

## 5. 邊界情境

| 情境 | 預期行為 |
|---|---|
| `holidays.txt` 不存在 | 視為無放假日，照常打卡，log 提醒檔案不存在 |
| 檔案存在但為空 | 視為無放假日，照常打卡 |
| 某行日期格式錯誤（如 `2026/08/21`） | 該行忽略 + log warning，其餘行正常生效 |
| 今天同時出現多次（重複行） | 視為同一天，正常判定為放假 |
| 檔案含 BOM 或非 UTF-8 編碼 | 讀取時以 UTF-8 開啟；若解碼失敗記錄錯誤並視為無放假日（不可讓解析錯誤阻擋非放假日的正常打卡） |
| 手動執行 `python auto_checkin.py in`，今天是放假日 | 一樣跳過（規則對手動 / 排程執行一視同仁，不特判執行來源） |

## 6. 驗收標準

- [ ] 在 `holidays.txt` 加入今天日期，執行 `python auto_checkin.py in`：終端機印出跳過訊息、`checkin.log` 有跳過紀錄、`hmsign.nsf` 沒有新文件、未觸發 Portal 登入、未發送 LINE 訊息。
- [ ] 將今天日期從 `holidays.txt` 移除（或檔案為空），執行同樣指令：行為與現有功能完全一致（正常簽到、Portal 登入、LINE 通知）。
- [ ] `holidays.txt` 不存在時，執行指令：行為與現有功能一致，且 log 有「找不到放假日清單」的提醒。
- [ ] `holidays.txt` 內含一行格式錯誤的日期，其餘日期仍正確生效，且今天不是放假日時仍正常打卡。

## 7. 實作任務清單

1. `auto_checkin.py` 設定區新增 `HOLIDAY_FILE` 常數（路徑比照 `LOG_FILE` 寫法）。
2. 新增 `is_holiday(check_date: date) -> bool` 函式（含檔案不存在 / 格式錯誤 / 編碼錯誤的容錯處理與 log）。
3. 在 `__main__` 區塊、呼叫 `checkin()` 之前插入放假日判斷與提早結束邏輯。
4. 專案根目錄新增 `holidays.txt`（含檔頭註解說明格式，內容留白或放入已知假日）。
5. 更新 `README.md`：於「功能一：自動打卡」段落補充放假日清單的說明與範例。
6. 依「6. 驗收標準」手動測試四種情境。

## 8. 待確認 / 後續可討論事項

- `holidays.txt` 是否要加入 `.gitignore`：內容屬個人排班／請假資訊，非機密但屬個人資料，建議與 `.env` 比照辦理排除追蹤（本階段先不預設，實作時再確認）。
